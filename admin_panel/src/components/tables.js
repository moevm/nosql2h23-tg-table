import React, {useEffect} from 'react';

const Tables = (props) => {
    useEffect(()=>{
        props.setTitle("Таблицы")
    })
    return (
        <div>

        </div>
    );
};

export default Tables;